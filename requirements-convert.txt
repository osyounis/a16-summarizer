# M2 Mac only (MLX is Apple-silicon). Confirm current flags with `mlx_lm.convert --help`.
mlx
mlx-lm
huggingface_hub

# Stage 4 MLX ROUGE eval (convert/eval_mlx_rouge.py) — reuses train/rouge_common.py.
# transformers arrives transitively via mlx-lm; datasets is needed by train/prepare_data.py
# to rebuild data/test.jsonl. Deliberately NO torch: the reused scoring path is torch-free.
datasets
evaluate
rouge_score
numpy
